# SEC-H05: 禁止Web直接访问config目录
# 防止配置文件（含数据库凭据等敏感信息）被直接下载

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
